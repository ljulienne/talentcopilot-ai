# Integration Guide — Release 7.3.0

The competency matrix is available from Candidate Intelligence. Data is persisted under `.talentcopilot_data/competency_matrices`, which should be backed by a durable store in production. The JSON implementation is a deterministic local persistence bridge for the current Streamlit product stage.
