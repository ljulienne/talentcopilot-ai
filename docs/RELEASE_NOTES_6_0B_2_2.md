# Release 6.0B.2.2 — Enterprise Workspace Engine Hotfix

## Fixes

- Replaces the invalid private-function test from 6.0B.2.1.
- Adds public `clamp_readiness`.
- Makes the installer safely rerunnable after a partial 6.0B.2.1 installation.
- Runs targeted UI tests with the repository root explicitly added to `PYTHONPATH`.
- Does not run the full test suite because this package is presentation-only.

## Scope

No matching, ranking, official scores, AI engines or business logic are modified.

## Required base

`c280c974dbd6bf7d626e8b3f58b3417c91d37478`
