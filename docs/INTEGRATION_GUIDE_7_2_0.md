# Integration Guide — Release 7.2.0

Expected base HEAD:

`6ea5a649482a910b596eed7e4be72e53160fcec0`

Run the installer from the extracted package. It copies only the release files, compiles them and runs the targeted regression test. Then run the full test suite in the Colab environment before commit and push.
