# Integration Guide — Release 7.0

The installer must be run from a repository synchronized to the expected GitHub snapshot. It copies only the release files, compiles the package and runs targeted tests. Run the full suite afterwards before commit and push.

Expected starting HEAD:

`b1fa47864e30aed9a6028e4becea6ece4ea29670`
